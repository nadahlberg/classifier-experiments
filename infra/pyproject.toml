[project]
name = "clx-infra"
version = "0.0.1"
requires-python = "==3.13.*"
dependencies = [
  "pulumi>=3,<4",
  "pulumi-digitalocean>=4.78,<5",
  "pulumi-docker-build",
  "pulumi-kubernetes>=4,<5",
  "pulumi-random>=4,<5",
]

[tool.uv]
package = false
