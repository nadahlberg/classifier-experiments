# Conventions

The house rules and the machinery that enforces them.

- [Pattern checks](checks.md) — how `manage check` runs the architecture
  assertions, the exemption ratchet, the deploy check, and the test suite
  that exercises every rule from both sides.
- [The rule catalog](rules.md) — every check id and what it enforces,
  family by family.
